implement the midnightexpress() function in @star_vii.py

for the algorithm, select vertical tracks or horizontal tracks (random boolean). then cut the grid in half,
lay track 1 in the middle of the left (or top) half, with some jitter (say, 25% up or down if horizontal track, 25% left or right if vertical track). then lay track 2 in the
middle of the right (or bottom) half of the grid, and add 25% jitter to it too. between the two tracks, place a few (between 3 and 10 of each color) random methuselahs, same
number of color1 as color2, all the same type, but pick a random methuselah from the list of 3.

methuselahs should come from the @b2s345c4_patterns directory

scaffoldunfusing, backedupsink, spaceship2platform are all good ones. 




